#!/bin/sh
# Unix-style pre-commit hook (for Git Bash on Windows)
# Calls PowerShell pre-commit.ps1
echo "[pre-commit] running (Git Bash wrapper)..."
powershell -ExecutionPolicy Bypass -File "$(dirname "$0")/pre-commit.ps1"
RC=$?
echo "[pre-commit] exit $RC"
exit $RC
